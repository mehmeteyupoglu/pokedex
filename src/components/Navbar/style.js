import { colors } from "../style";

//NAVBAR
export const navbar = {
  boxShadow: `1px 3px 10px ${colors.gray}`,
  marginBottom: "3rem",
};

export const darkNavbar = {
  borderBottom: `1px solid ${colors.gray}`,
  backgroundColor: colors.cardDBg,
  marginBottom: "3rem",
};

export const navbarRightStyle = {
  display: "flex",
  alignItems: "center",
};
